<header>
  <div class="up_page">
    <a href="/home"><img
        class="logo"
        src="https://i.postimg.cc/JzVpc1S5/logo-header.png"
        alt="logo"
        width="80"
      /></a>
  </div>
  <nav class="navbar">
    <a href="/home">Home</a>
    <a href="/products">Products Data</a>
    <a href="/realtimeproducts">Update Products</a>
    <a href="/chat">Chat</a>
  </nav>


  <h2 class="cart_main" id="cart" data-cid="{{cartId}}">Cart</h2>

{{#each cart.products}}
<div class="product_detail">
  <div class="cart">
    <img src="{{product.thumbnail}}" class="image_cart" alt="{{product.title}}" />
    <div class="cart-body">
      <h3 class="cart_title"> {{product.title}}</h3>
      <p>Price: ${{product.price}}</p>
      <p>Quantity: {{quantity}}</p>
    </div>
  </div>
</div>
    {{/each}}
      </div>
    </div>
    </main>

<footer></footer>